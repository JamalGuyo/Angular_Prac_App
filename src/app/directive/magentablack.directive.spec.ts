import { MagentablackDirective } from './magentablack.directive';

describe('MagentablackDirective', () => {
  it('should create an instance', () => {
    const directive = new MagentablackDirective();
    expect(directive).toBeTruthy();
  });
});
